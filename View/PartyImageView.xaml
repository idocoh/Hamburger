<Page
    x:Class="MesibaViewer.View.PartyImageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MesibaViewer.View"
    xmlns:ViewModel="clr-namespace:MesibaViewer.ViewModel"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel>
            <FlipView ItemsSource="{x:Bind partyImageViewModel.listOfItems}" SelectedIndex="{Binding currentIndex, Mode=OneWayToSource}" SelectionChanged="{x:Bind partyImageViewModel.SelectionChanged}"/>
            <StackPanel Orientation="Horizontal" VerticalAlignment="Bottom" HorizontalAlignment="Center" Height="100" Canvas.ZIndex="1" Background="White"/>
        </StackPanel>
    </Grid>
</Page>
